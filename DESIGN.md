# DESIGN.md — Vigsharm (v3, гибрид)

Источник правды для витрины. Совпадает с тем, что уже на экране, а не с черновиками v1/v2.

## Язык
**Фото товара + 3D только как иконка.**  
Живые (или студийные) фото композиций — hero, каталог, карточка, блок «о студии».  
Мягкий clay/3D — меню, чипы групп, прайс/доставка, бюджет, recipient-тайлы, шаги заказа.  
3D **не** вместо фото композиции, **не** на кнопках, **не** в chrome шапки/футера.

Реализация стиля витрины: [`assets/site-marshmallow.css`](assets/site-marshmallow.css) (подключать последним). Не дублировать токены в `style.css` / `home-polish.css`.

## Токены (как в CSS)
```
--mm-bg:            #ffffff
--mm-ink:           #1e293b
--mm-muted:         #475569
--mm-radius:        22px
--mm-radius-lg:     24px
--mm-shadow:        0 10px 30px rgba(0,0,0,.05), 0 2px 8px rgba(0,0,0,.02)
--mm-shadow-hover:  0 16px 40px rgba(0,0,0,.08), 0 4px 12px rgba(0,0,0,.03)
--mm-accent-grad:   linear-gradient(135deg, #ff6b6b, #ff8e53)
--mm-focus:         0 0 0 4px rgba(255,107,107,.16)
--accent (логотип): #FF6B5A
```

- Страница и карточки — белый `#fff`. Не серый лист.
- Primary CTA — градиент `--mm-accent-grad` + коралловая тень. Не заливать кнопку радугой логотипа.
- Ссылки, номера FAQ, outline-акцент — коралл логотипа `#FF6B5A`.
- Не выравнивать `#ff6b6b` к `#FF6B5A` без явного «ок».
- Поля: светлый фон, радиус 12–16px, фокус `--mm-focus`, не синяя рамка браузера.
- Пастель групп (мята / персик / лаванда / жёлтый) на прайсе и доставке — можно. Это не радуга логотипа.

## Логотип
Wordmark «ВИГШАРМ» — радуга по буквам. Подпись «СТУДИЯ АЭРОДИЗАЙНА» — графит `#4A4A52`.

Можно: логотип как есть; pill «ВСЁ!»; одна короткая искорка.  
Нельзя: семь цветов на кнопки, карточки, иконки; радужный фон секции; радужный CTA.

Мессенджеры (только кнопки связи): WhatsApp `#25D366`, Telegram `#229ED9`.

## Типографика
Стек из `assets/fonts.css`. Заголовки жирные, `letter-spacing: -0.03em`. Текст 15–17px, `line-height: 1.5`. Eyebrow uppercase, tracking ~0.06em. Не ставить Inter «потому что красиво».

## Форма
Секции кратны 8px. Карточки 20–24px. Primary — капсула `border-radius: 999px`, высота 48–52px. Outline — белый фон, тонкая рамка, текст `--mm-ink`. Тень — у карточки и hover, не под каждым абзацем.

## Иконки
- **3D PNG** (~1024 квадрат, объект целиком, белый или прозрачный фон, без макро-кропа). Один смысл — один файл. Не вешать один грузовик на город / загород / оплату / самовывоз.
- **Line SVG** — телефон, пин, чат, «наверх», соцсети в chrome. Монохром `#2A2A32` / коралл. Не toon-эмодзи и не ✦ в футере.
- Меню: 3D PNG ок.

## Запрещено
- Фиолетовые «AI» градиенты, неон.
- Корзина и оплата картой на сайте.
- Массово переименовывать ~157 товаров.
- Красить админку в этом же круге, что витрину.
- Менять токены без согласования.

## Мобильный фокус
Проверка **375px**. CTA min-height 48px. Sticky не перекрывает контент.

## Агенту
1. Читать этот файл перед экраном.
2. Один блок за раз.
3. Новые цвета — только из токенов или после «ок».
4. После блока — 375px + список «Запрещено».
